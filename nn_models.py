# Imports

# Note funtion should return Accuracy, loss, precision, f1_score, recall and multiple layers
# output softmax or sigmoid


def lstm_model(X_train,y_train):
    #lets write logic for model here
    return {
    "loss": loss,
    "accuracy": accuracy,
    "precision": precision,
    "recall": recall,
    "f1_score": f1_score}


def lr_model(X_train,y_train):
    #lets write logic for model here
    return {
    "loss": loss,
    "accuracy": accuracy,
    "precision": precision,
    "recall": recall,
    "f1_score": f1_score}


def random_tree(X_train,y_train):
    #lets write logic for model here
    return {
    "loss": loss,
    "accuracy": accuracy,
    "precision": precision,
    "recall": recall,
    "f1_score": f1_score}
